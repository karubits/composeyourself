# Porkbun DDNS (mietzen/porkbun-ddns) – copy to .env and set values
# Updates Porkbun DNS records with your current public IP(s).
# One domain per container; use SUBDOMAINS for multiple subdomains of that domain.
# For multiple domains (same IP): run another container with a separate .env (e.g. .env.domain2).
# https://github.com/mietzen/porkbun-ddns

# Your Porkbun domain (one per .env / container)
DOMAIN=domain.com

# Subdomains to update (comma-separated)
SUBDOMAINS=www,home,subdomain

# Porkbun API credentials (from Porkbun dashboard)
APIKEY=your-api-key
SECRETAPIKEY=your-secret-api-key

# Optional: static IP(s) instead of auto-detect (comma-separated, IPv4 and/or IPv6)
# PUBLIC_IPS=1.2.3.4,2001:db8::1

# Optional: Fritz!Box router IP to obtain public IP from
# FRITZBOX=192.168.178.1

# Optional: seconds between DDNS update runs
# SLEEP=300

# Optional: enable/disable IPv4 and IPv6 updates
# IPV4=TRUE
# IPV6=TRUE

# Optional: enable debug logging
# DEBUG=FALSE
