# Porkbun DDNS - Sample .env file
# Copy or rename this file to `.env` and adjust values as needed.
# See: https://github.com/mietzen/porkbun-ddns

# -----------------------------------------------------------------------------
# Domain and subdomains (required)
# -----------------------------------------------------------------------------
# Your Porkbun domain (one per .env / container)
DOMAIN=domain.com
# Subdomains to update (comma-separated)
SUBDOMAINS=www,home,subdomain

# -----------------------------------------------------------------------------
# Porkbun API (required)
# -----------------------------------------------------------------------------
# From Porkbun dashboard
APIKEY=your-api-key
SECRETAPIKEY=your-secret-api-key

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# Static IP(s) instead of auto-detect (comma-separated, IPv4 and/or IPv6)
# PUBLIC_IPS=1.2.3.4,2001:db8::1

# Optional: Fritz!Box router IP to obtain public IP from
# FRITZBOX=192.168.178.1

# Optional: seconds between DDNS update runs
# SLEEP=300

# Optional: enable/disable IPv4 and IPv6 updates
# IPV4=TRUE
# IPV6=TRUE

# Optional: enable debug logging
# DEBUG=FALSE
