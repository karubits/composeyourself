# Monica - Sample .env file
# Copy or rename this file to `.monica.env` (used by compose) and adjust values.
# App and DB variables: see https://github.com/monicahq/docker#environment-variables

# -----------------------------------------------------------------------------
# Domain and Traefik (optional)
# -----------------------------------------------------------------------------
# Used for router rule Host(`monica.${DEFAULT_DOMAIN}`)
DEFAULT_DOMAIN=example.com

# -----------------------------------------------------------------------------
# Application and database (required)
# -----------------------------------------------------------------------------
# Add APP_KEY, DB_*, and other vars from Monica’s env.example to .monica.env.
# See: https://github.com/monicahq/docker#environment-variables
