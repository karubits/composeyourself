# Code Server - Sample .env file
# Copy or rename this file to `.env` and adjust values as needed.
# See: https://docs.linuxserver.io/images/docker-code-server/

# -----------------------------------------------------------------------------
# Domain and Traefik (optional)
# -----------------------------------------------------------------------------
# Base domain for Host(`code.${DEFAULT_DOMAIN}`)
DEFAULT_DOMAIN=example.com

# -----------------------------------------------------------------------------
# Paths (required for compose volume mounts)
# -----------------------------------------------------------------------------
PATH_CONFIG=/path/to/config
PATH_STACKS=/path/to/stacks
# Optional: GitHub deploy key for git operations from code-server
# PATH_GIT_DEPLOY_KEY=/path/to/github-deploy

# -----------------------------------------------------------------------------
# User and auth (optional)
# -----------------------------------------------------------------------------
PUID=1000
PGID=1000
TZ=Europe/London
# Web UI login password (optional; if unset, auth may be disabled)
# PASSWORD=your-secure-password
