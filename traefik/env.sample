# Traefik - Sample .env file
# Copy or rename this file to `.env` and adjust values as needed.
# See: https://doc.traefik.io/traefik/reference/static-configuration/env/

# -----------------------------------------------------------------------------
# Domain and paths (required)
# -----------------------------------------------------------------------------
DEFAULT_DOMAIN=example.com
PATH_DATA=/path/to/data

# -----------------------------------------------------------------------------
# Dashboard auth (required)
# -----------------------------------------------------------------------------
# Basic auth for Traefik dashboard (htpasswd format: user:hashedpassword)
# Generate: htpasswd -nb admin yourpassword
TRAEFIK_DASHBOARD_PASS=admin:$apr1$...
# Homepage widget password (optional; for Traefik widget)
TRAEFIK_PASS=your-dashboard-password

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
TZ=Europe/London
# For Porkbun DNS challenge (if using in traefik.yml)
# PORKBUN_API_KEY=
# PORKBUN_SECRET_API_KEY=
