# Seerr - Sample .env file
# Copy or rename this file to `.env` and adjust values as needed.
# See: https://docs.seerr.dev/ and https://docs.seerr.dev/extending-seerr/database-config

# -----------------------------------------------------------------------------
# Traefik / domain (required for router rule Host(`seerr.${DEFAULT_DOMAIN}`))
# -----------------------------------------------------------------------------
DEFAULT_DOMAIN=example.com

# -----------------------------------------------------------------------------
# Paths (optional – used for volume mount)
# -----------------------------------------------------------------------------
# PATH_CONFIG=/path/to/config

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
TZ=Europe/London

# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=info

# Port is fixed in compose (5055); only change if you alter the service port
# PORT=5055

# -----------------------------------------------------------------------------
# Database – SQLite (default)
# -----------------------------------------------------------------------------
# No env vars needed. Seerr uses SQLite in /app/config by default.
# Optional overrides:
# DB_TYPE=sqlite
# CONFIG_DIRECTORY=config
# DB_LOG_QUERIES=false

# -----------------------------------------------------------------------------
# Database – PostgreSQL (optional)
# -----------------------------------------------------------------------------
# Uncomment and set DB_TYPE=postgres, then add a postgres service to compose
# and wire DB_HOST to that service name (e.g. db).
# DB_TYPE=postgres
# DB_HOST=db
# DB_PORT=5432
# SEERR_DB_USER=seerr
# SEERR_DB_PASS=changeme
# SEERR_DB_NAME=seerr
# DB_LOG_QUERIES=false
# DB_USE_SSL=false
