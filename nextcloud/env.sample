# Nextcloud - Sample .env file
# Copy or rename this file to `.env` and adjust values as needed.
# See: https://docs.nextcloud.com/server/stable/admin_manual/installation/docker/

# -----------------------------------------------------------------------------
# Domain and paths (required)
# -----------------------------------------------------------------------------
DEFAULT_DOMAIN=example.com
PATH_DATA=/path/to/data
# Nextcloud data directory (often same as PATH_DATA/nextcloud or a dedicated volume)
NEXTCLOUD_DATA_DIR=/path/to/nextcloud/data

# -----------------------------------------------------------------------------
# Nextcloud admin (required – set on first run)
# -----------------------------------------------------------------------------
# NEXTCLOUD_ADMIN_USER=admin
# NEXTCLOUD_ADMIN_PASSWORD=changeme

# -----------------------------------------------------------------------------
# Database – MySQL (required – single shared secret for db and app)
# -----------------------------------------------------------------------------
# Use one variable for both MYSQL_PASSWORD and app connection.
MYSQL_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=changeme
# MYSQL_USER=nextcloud and MYSQL_DATABASE=nextcloud are fixed in compose.

# -----------------------------------------------------------------------------
# Redis cache (required)
# -----------------------------------------------------------------------------
REDIS_HOST_PASSWORD=changeme

# -----------------------------------------------------------------------------
# SMTP (optional)
# -----------------------------------------------------------------------------
# SMTP_SERVER=smtp.example.com
# SMTP_USER=
# SMTP_PASS=

# -----------------------------------------------------------------------------
# Nextcloud Talk and TURN (optional – for calls/screenshare)
# -----------------------------------------------------------------------------
# Set SERVER_PUBLIC_IP if auto-detect fails for TURN.
# SERVER_PUBLIC_IP=
TURN_SECRET=changeme-turn-secret
TALK_JWT_SECRET=changeme-talk-jwt
TALK_SIGNALING_SECRET=changeme-signaling-secret

# -----------------------------------------------------------------------------
# OnlyOffice (optional – document server)
# -----------------------------------------------------------------------------
ONLYOFFICE_JWT_SECRET=changeme-onlyoffice-jwt

# -----------------------------------------------------------------------------
# Homepage widget (optional)
# -----------------------------------------------------------------------------
# TOKEN_SERVICE_INFO=

# Tip: If you are using authentik follow this guide for SSO.
# https://integrations.goauthentik.io/productivity/nextcloud
