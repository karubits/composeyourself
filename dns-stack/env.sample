# DNS Stack - Sample .env file
# Copy or rename this file to `.env` and adjust values as needed.
# See: https://doc.powerdns.com/authoritative/
# See: https://github.com/AdguardTeam/AdGuardHome

# -----------------------------------------------------------------------------
# Domain and paths (required)
# -----------------------------------------------------------------------------
DEFAULT_DOMAIN=example.com
PATH_CONFIG=/path/to/config
PATH_DATA=/path/to/data

# -----------------------------------------------------------------------------
# PowerDNS API (required)
# -----------------------------------------------------------------------------
# API key for pdns-auth containers (same key can be used for both if desired)
API_KEY_PDNS_AUTH=your-pdns-api-key

# -----------------------------------------------------------------------------
# AdGuard Home bind IPs (required)
# -----------------------------------------------------------------------------
# LAN-facing IP for local AdGuard instance (e.g. 192.168.1.2)
IP_LAN=192.168.1.2
# Overlay/second IP for second AdGuard instance if used
IP_OVERLAY=192.168.1.3
